//TODO: build out a more robust config that changes the cms config.yml file programmatically based on 
//the environment. ie development vs production
export const config = {
  domain: 'https://www.jasonmason.dev'
}
